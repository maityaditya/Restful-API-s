<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update the existing data</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Update Content</h1>
    <div>
    <p><span class="label">Id: </span><%= post.id %></p>
    <p><span class="label">Username: </span><%= post.username %></p>
    <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
        <p>
            <span class="label">Content: </span>
            <textarea placeholder="enter content" name="content" rows="5" cols="45"><%= post.content %></textarea>
        </p>
        <button>Submit</button>
    </form>
    </div>
</body>
</html>