<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Show all posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora</h1>
    <% for(let post of posts){ %>
        <div>
            <p><span class="label">username: </span><%= post.username %></p>
            <p><span class="label">content: </span> <%= post.content %></p>
            <a href="http://localhost:8080/posts/<%= post.id %>">See detail post</a>
            <a href="http://localhost:8080/posts/<%= post.id %>/new">Update detail</a>
            <br><br>
            <form method="post" action="http://localhost:8080/posts/<%= post.id %>?_method=DELETE">
                <button>Delete post</button>
            </form>
        </div>
    <% } %>
    <a href="http://localhost:8080/posts/new">Create a new post</a>
</body>
</html>